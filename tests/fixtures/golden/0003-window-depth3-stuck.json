{
  "problem_id": "0003-event-log-uniqueness-window",
  "current_code": "def event_log_uniqueness_window(events):\n    left = 0\n    # how do I know when to move left?\n    pass",
  "depth": 3,
  "assertions": {
    "must_not_contain_code_fence_over_3_lines": true,
    "must_not_contain": ["seen[event] =", "left = seen[", "max_len = max("],
    "concept": "When a duplicate appears inside the current window, moving the left boundary forward until the window is valid again."
  }
}
