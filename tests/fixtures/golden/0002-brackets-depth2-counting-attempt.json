{
  "problem_id": "0002-bracketed-template-validator",
  "current_code": "def bracketed_template_validator(template):\n    return template.count('(') == template.count(')')",
  "depth": 2,
  "assertions": {
    "must_not_contain_code_fence_over_3_lines": true,
    "must_not_contain": ["stack.append", "stack.pop", "match = {"],
    "concept": "Using a last-in-first-out stack data structure so the matching opener for each closer is available at the top."
  }
}
