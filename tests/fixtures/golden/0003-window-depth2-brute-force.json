{
  "problem_id": "0003-event-log-uniqueness-window",
  "current_code": "def event_log_uniqueness_window(events):\n    best = 0\n    for i in range(len(events)):\n        for j in range(i, len(events)):\n            if len(set(events[i:j+1])) == j-i+1:\n                best = max(best, j-i+1)\n    return best",
  "depth": 2,
  "assertions": {
    "must_not_contain_code_fence_over_3_lines": true,
    "must_not_contain": ["left = ", "right += ", "while events"],
    "concept": "Replacing brute force with a sliding-window or two-pointer approach that adjusts the current range as duplicates appear."
  }
}
